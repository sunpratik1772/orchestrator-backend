# Required for AI features (Copilot + agent node)
GOOGLE_API_KEY=

# Slack — bot token preferred (uses chat.postMessage); falls back to webhook URL.
SLACK_API_TOKEN_NOW=
# SLACK_BOT_TOKEN=        # alias for SLACK_API_TOKEN_NOW
# SLACK_WEBHOOK_URL=      # fallback if no bot token

# Optional integrations
GITHUB_TOKEN=
NOTION_API_KEY=
GMAIL_CLIENT_SECRET=
MCP_SERVER_URL=

# Server config
PORT=8080
ALLOWED_ORIGINS=*
